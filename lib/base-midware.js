/**
 * The base Middleware Class, all Middlewares extend from.
 */

import {EventEmitter} from 'events';

/**
 * The base Middleware Class, all Middlewares extend from.
 *
 * @extends {events.EventEmitter}
 */
 export default class BaseMidware extends EventEmitter {};

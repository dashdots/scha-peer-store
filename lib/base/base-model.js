/**
 * The base Model Class, all Middlewares extend from.
 */

import {EventEmitter} from 'events';

/**
 * The base Model Class, all Middlewares extend from.
 *
 * @extends {events.EventEmitter}
 */
 export default class BaseModel extends EventEmitter {};
